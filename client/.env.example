# If your API is NOT on port 5000, set this so Vite can proxy /api to the right place.
# Then restart: npm run dev
# VITE_API_PROXY_TARGET=http://127.0.0.1:5001
#
# Production only: full API origin if frontend and API are on different hosts.
# VITE_API_URL=https://your-api.example.com
