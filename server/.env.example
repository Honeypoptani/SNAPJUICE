# Use 5050 by default — port 5000 on Mac is often taken by AirPlay (requests then get 403).
PORT=5050
MONGODB_URI=mongodb+srv://<username>:<password>@cluster.mongodb.net/snapjuice?retryWrites=true&w=majority
JWT_SECRET=replace_with_a_long_random_secret
